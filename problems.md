# problems to fix

* Voigt peak is ~twice as wide as pseudo-Voigt with same parameters
* Electron mode: peak heights are multiplicity-only — no electron scattering
  factor f_e(s), so relative intensities are geometric, not quantitative
  (low-g reflections should dominate once f_e(s) is added)
